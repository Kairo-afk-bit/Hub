local function LoadGame(password, rawUrl, targetId, isEnabled)  
    if not isEnabled or game.PlaceId ~= targetId then return end
    local success, content = pcall(function()
        return game:HttpGet(rawUrl)
    end)
    if success then
        local run = loadstring(content)
        if run then
            local env = getfenv(run)
            env.Password = password
            run()
        end
    end
end


LoadGame("ID]7HeRVs3pSqzM+SOlr57knutw]U2l5", "https://raw.githubusercontent.com/Dynamao/Hub/refs/heads/main/Script/Build%20And%20Survive", 128263975853774, true)
